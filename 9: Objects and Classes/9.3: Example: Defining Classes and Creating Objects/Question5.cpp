/*
Objects of the Window class require a width(int) and a height(int) be specified (in that order) upon creation.
Declare an object named window, of type Window, whose width and length are obtained from the (already declared and initialized) variables, w and l.
*/

Window window (w,l);
