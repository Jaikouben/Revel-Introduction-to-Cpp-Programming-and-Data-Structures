/*
Write an if/else statement that compares the variable age with 65, adds 1 to the variable seniorCitizens if age is greater than or equal to 65, and adds 1 to the variable nonSeniors otherwise. Assume all variables have been declared and initialized.
*/

if (age >= 65)
    seniorCitizens += 1;
else
    nonSeniors += 1;
