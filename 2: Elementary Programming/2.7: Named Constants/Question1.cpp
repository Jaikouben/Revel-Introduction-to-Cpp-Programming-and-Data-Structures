/*
Declare an int constant, MonthsInYear, whose value is 12.
Note: The quizzes are a third-party product. The name for the constant does not follow the convention used in the text. It would be named MONTHS_IN_YEAR if the convention in the text is used. This is the only place we alert our users. We will not explicitly point it out in other places.
*/

const int MonthsInYear = 12;
