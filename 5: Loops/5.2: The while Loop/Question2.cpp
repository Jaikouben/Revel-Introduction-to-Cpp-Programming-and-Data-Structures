/*
Given an int variable n that has already been declared, write some code that repeatedly reads a value into n until at last a number between 1 and 10 (inclusive) has been entered. Don't forget to initialize n with an appropriate value. 
*/

while (n > 10 || n < 1)
{
    cin >> n;
}
