/*
Write a statement that declares a prototype for a function powerTo, which has two parameters.
The first is a double and the second is an int. The function returns a double.
*/

double powerTo(double a, int b);
